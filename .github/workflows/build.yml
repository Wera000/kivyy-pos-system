name: Build Kivy Android App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # ✅ 1. CHECKOUT REPO
    - name: Checkout Code
      uses: actions/checkout@v4

    # ✅ 2. SYSTEM DEPENDENCIES
    - name: Install System Dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          git zip unzip wget openjdk-17-jdk \
          build-essential zlib1g-dev libffi-dev libssl-dev
        git --version
        java -version

    # ✅ 3. PYTHON 3.9
    - name: Set up Python 3.9
      uses: actions/setup-python@v5
      with:
        python-version: "3.9"

    # ✅ 4. BUILDOZER + CYTHON
    - name: Install Buildozer
      run: |
        pip install --upgrade pip
        pip install cython buildozer setuptools==65.5.0

    # ✅ 5. INSTALL ANDROID SDK + NDK (AIDL HARD FIX)
    - name: Install Android SDK & NDK
      run: |
        mkdir -p $HOME/android-sdk
        cd $HOME/android-sdk

        wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
        unzip commandlinetools-linux-11076708_latest.zip

        mkdir -p cmdline-tools/latest
        mv cmdline-tools/* cmdline-tools/latest || true

        export ANDROID_HOME=$HOME/android-sdk
        export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH

        yes | sdkmanager --licenses

        sdkmanager \
          "platform-tools" \
          "platforms;android-30" \
          "build-tools;30.0.3" \
          "ndk;21.4.7075529"

        echo "===== AIDL DEBUG ====="
        find $ANDROID_HOME -name aidl
        ls -l $ANDROID_HOME/build-tools/30.0.3/aidl

    # ✅ 6. FORCE CLEAN BROKEN CACHE
    - name: Destroy Buildozer Cache
      run: |
        rm -rf ~/.buildozer
        rm -rf ~/.gradle

    # ✅ 7. BUILD APK
    - name: Build APK
      env:
        ANDROID_HOME: $HOME/android-sdk
        JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
      run: |
        export PATH=$PATH:$HOME/android-sdk/cmdline-tools/latest/bin:$HOME/android-sdk/platform-tools
        buildozer android debug

    # ✅ 8. UPLOAD APK
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Kivy-Android-App
        path: bin/*.apk
